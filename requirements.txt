fastapi
# Pinned: mcp 2.0.0 REMOVED `mcp.server.fastmcp`, which this server is built on —
# an unpinned install gets 2.0.0 and dies at import with
# "ModuleNotFoundError: No module named 'mcp.server.fastmcp'". 1.x still ships it.
# Porting to the 2.x server API is tracked separately; until then stay on 1.x.
mcp>=1.13,<2
uvicorn
httpx
python-dotenv
markdown-it-py
mdit-py-plugins
# Pinned: pycrdt 0.14.1 regressed collab decoding — it raises "Cannot decode
# update: while reading, an unexpected value was found" on some AppFlowy *database*
# collabs (type 1) that 0.13.0 and every earlier version decode fine. That silently
# broke all Tier-2 structural ops (delete_row, field/option edits) on affected
# boards while row-collab cell writes still worked. Stay on 0.13.0 until fixed
# upstream (https://github.com/y-crdt/pycrdt).
pycrdt==0.13.0
